<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN'
        'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>
<mapper namespace='com.kayak.web.workflow.mapper.CommonMapper'>
    <select id="listBySql" parameterType="string" resultType="string">
        ${sql}
    </select>
    <select id="getBySqlRetStr" parameterType="string" resultType="string">
        ${sql}
    </select>
    <select id="getBySqlRetInteger" parameterType="string" resultType="Integer">
        ${sql}
    </select>
    <update id="updateBySql" parameterType="string">
        ${sql}
    </update>
    <select id="getBySqlRetObject" resultType="java.lang.Object">
        ${sql}
    </select>
    
    <select id="getBySqlRetList" resultType="java.lang.Object">
        ${sql}
    </select>
</mapper>