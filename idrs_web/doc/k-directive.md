---

>	Description: 指令控件

>	Author: 李博

---
参数格式：JSON字符串

属性：
-	data-type：指令类型，login-user：获取当前登录用户信息，auth：权限控制，用于显示和隐藏，system-params:系统参数
-	data-value-field: 当指令是获取数据时，该属性用于指定获取数据的字段
-	data-auth: 当指令是权限控制时，该属性用于指定权限action
